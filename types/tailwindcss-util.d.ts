declare module 'tailwindcss/lib/util/flattenColorPalette' {
  // If you know the exact type, replace 'any' with the appropriate type
  const flattenColorPalette: (colors: any) => any;
  export default flattenColorPalette;
}
